EguiContext = (
    | handle |

    handle = ( ^handle )
    handle: h = ( handle := h. )

    window: title do: block = primitive

    isKeyDown: keyName = primitive
    requestRepaint = primitive
)
